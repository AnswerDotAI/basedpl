# `⍝` — Comment

`⍝` comments to end of line.

```text
v←⍳10     ⍝ first ten integers
+/v       ⍝ sum
```

Inside character literals it is an ordinary character.

```apl
'a⍝b'             ⍝ 'a⍝b'
```
